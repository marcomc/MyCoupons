{
  "name": "mycoupons",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Private Google Apps Script Gmail coupon importer",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check.mjs"
  },
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  }
}
