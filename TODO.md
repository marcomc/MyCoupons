# Future deliverables

## Chrome extension

Build a private Chrome extension that displays authenticated coupon data using
the selected private-access design. Define consent, token storage, revocation,
and minimum data exposure before implementation.

## Safari extension

Build a private Safari extension with feature parity appropriate to Safari's
extension model after the Chrome access contract is stable. Define its signing,
distribution, and authenticated data-access boundaries before implementation.
